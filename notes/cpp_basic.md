# C++ 基础检查表

## 第一阶段

- 基本类型、作用域、条件和循环
- 函数声明与定义
- `std::string`、`std::vector` 和范围 `for`
- `struct` 与 `class`
- 头文件与源文件

## 与 Python 差异最大的概念

- 值语义与对象复制
- 左值引用 `T&` 与只读引用 `const T&`
- 指针、空指针与对象生命周期
- 栈对象、动态分配与 RAII
- 编译单元、编译、链接和 ABI

第一阶段优先使用标准容器和自动生命周期管理，不主动写裸 `new`/`delete`。
